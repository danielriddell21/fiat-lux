// Package world holds the entity registry: the World, Entity, and
// Relationship types that make up a fiat-lux universe.
//
// A World begins empty. Everything that ever exists in it was created
// by an agent via a tool call. Types, properties, and relationship
// kinds are agent-chosen free-form strings; the simulation does not
// interpret them.
package world
