// Package agent defines the runtime brain attached to an agent-entity:
// its identity, perception assembly, and the per-tick decision loop.
//
// Every Agent is also an Entity inside some World, so it can be
// observed and related to like any other thing.
package agent
